{{>local_uitest/intro}}

<hr>

<!-- Make sure the CSS you create is injected in this template !-->
{{>local_uitest/injectcss}}

<div class="" role="dialog" tabindex="-1">
    <div class="modal-dialog modal-dialog-scrollable modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    Choose a teacher
                </h5>
                <button aria-label="Close" class="close" data-dismiss="modal" type="button">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body container-fluid py-0">
                <div class="contacts row px-2 py-4 bg-light">
                    {{#context.contacts}}
                        {{> local_uitest/workshop_contacts }}
                    {{/context.contacts}}
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn XXX" data-dismiss="modal" type="button">Cancel</button>
            </div>
        </div>
    </div>
</div>

{{>local_uitest/cssoutput}}